#!/usr/bin/env bash
# Pull the latest dotfiles and refresh submodules.

set -euo pipefail

cd "$HOME"

branch="$(git symbolic-ref --short HEAD 2>/dev/null || echo dotfiles)"

git fetch --quiet origin "$branch"
git merge --ff-only "origin/$branch"
git submodule update --init --recursive --jobs 8
